import React from 'react';
import { WorkspaceHeader } from '../../../components/WorkspaceHeader';

export default function JsonWorkspacePage() {
  return (
    <div>
      <WorkspaceHeader title="JSON" subtitle="Preview and export the unified application bundle." />
      <p>Placeholder for JSON preview/export tools.</p>
    </div>
  );
}
