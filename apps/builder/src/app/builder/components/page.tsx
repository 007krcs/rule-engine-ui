import React from 'react';
import { WorkspaceHeader } from '../../../components/WorkspaceHeader';

export default function ComponentsWorkspacePage() {
  return (
    <div>
      <WorkspaceHeader title="Components" subtitle="Browse and configure reusable UI components." />
      <p>Placeholder for component catalog workspace.</p>
    </div>
  );
}
