import React from 'react';
import { WorkspaceHeader } from '../../../components/WorkspaceHeader';

export default function RepoWorkspacePage() {
  return (
    <div>
      <WorkspaceHeader title="Repo" subtitle="Explore curated repositories and highlighted files." />
      <p>Placeholder for repository workspace.</p>
    </div>
  );
}
