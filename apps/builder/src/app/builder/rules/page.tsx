import React from 'react';
import { WorkspaceHeader } from '../../../components/WorkspaceHeader';

export default function RulesWorkspacePage() {
  return (
    <div>
      <WorkspaceHeader title="Rules" subtitle="Define conditional logic and guard evaluations." />
      <p>Placeholder for rules builder UI.</p>
    </div>
  );
}
