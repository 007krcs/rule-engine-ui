# WCAG

Accessibility is mandatory.

- `ariaLabelKey` required for every component
- `keyboardNav` must be true
- `focusOrder` is mandatory and must be unique

CI fails when accessibility metadata is missing.