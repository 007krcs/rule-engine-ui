# Security

RuleFlow enforces:

- Tenant isolation (`tenantId` required)
- No dynamic code execution
- Timeouts + max rules + circuit breakers
- Audit trails and replay

All schemas and config transitions are validated and signed.