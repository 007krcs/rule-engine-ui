# Adapters

Adapters map `adapterHint` values to real UI components.

```ts
registerAdapter('acme.', (component, ctx) => {
  const aria = ctx.i18n.t(component.accessibility.ariaLabelKey);
  return <AcmeWidget aria-label={aria} />;
});
```

You can ship adapters for any library without changing the runtime.