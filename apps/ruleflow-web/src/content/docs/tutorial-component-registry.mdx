# Tutorial: Component Registry

The Component Registry is how RuleFlow stays pluggable:
- the platform runtime is headless
- UI is rendered by adapters resolved via `adapterHint`
- Builder learns how to edit props from each component's `propsSchema`

## What a component definition looks like

Component manifests are JSON:

```json
{
  "schemaVersion": 1,
  "components": [
    {
      "adapterHint": "company.currencyInput",
      "displayName": "Currency Input",
      "category": "Company",
      "propsSchema": {
        "type": "object",
        "properties": {
          "label": { "type": "string", "title": "Label" },
          "currency": { "type": "string", "enum": ["USD", "EUR"] }
        }
      },
      "defaultProps": { "label": "Amount", "currency": "USD" },
      "bindings": { "data": ["value"] }
    }
  ]
}
```

## Step-by-step (5 minutes)

1. Open the registry page
- Navigate to `/component-registry`.

2. Choose scope
- **Global** applies to all tenants in the demo.
- **Tenant** overrides global definitions for a specific tenantId.

3. Paste a manifest
- Paste the JSON into **Manifest JSON**.
- Fix validation issues if any are shown.

4. Register
- Click **Register**.
- Go to Builder: the palette should include the newly registered components immediately.

## Screenshot placeholders

- [TODO screenshot: Component Registry page]

