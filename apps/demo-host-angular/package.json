{
  "name": "demo-host-angular",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@platform/angular-renderer": "workspace:*",
    "@platform/core-runtime": "workspace:*",
    "@platform/i18n": "workspace:*",
    "@platform/schema": "workspace:*",
    "@platform/web-component-bridge": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  }
}
