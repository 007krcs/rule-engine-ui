# Demo Host Vue

Vue-oriented demo host using the HTML renderer.

Purpose
Demonstrate how a host app executes a flow step and renders the resulting UI schema.

Exports
- `runVueDemo` to execute the demo flow and render HTML

When to modify
Add Vue-specific hydration or routing integration.

When not to touch
Do not bypass runtime validation or accessibility checks.
