# Demo Host React

React demo host with runtime orchestration and adapter rendering.

Purpose
Show a full end-to-end flow execution using the React renderer and adapters.

Exports
- `App` component and `demo` script for CLI use

When to modify
Add new demo scenarios, API stubs, or adapter registrations.

When not to touch
Do not remove trace output or core runtime calls used in tests.
