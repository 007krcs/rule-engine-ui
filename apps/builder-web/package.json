{
  "name": "builder-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@platform/schema": "workspace:*",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  }
}
