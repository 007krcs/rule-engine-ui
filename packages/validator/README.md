# Validator

Schema validation, accessibility checks, and i18n coverage enforcement.

Purpose
Fail fast on invalid configuration, missing translations, or accessibility violations.

Exports
- `validate*` and `assert*` helpers for schemas and accessibility

When to modify
Add new validation rules or additional schema checks.

When not to touch
Do not weaken accessibility enforcement or schema checks without governance approval.
