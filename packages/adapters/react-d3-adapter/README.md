# React D3 Adapter

Inline SVG sparkline adapter for `d3.chart`.

Purpose
Render lightweight line charts without external dependencies.

Exports
- `registerD3Adapter` to register the adapter

When to modify
Add new series styles or annotations.

When not to touch
Do not remove aria labels or chart fallbacks.
