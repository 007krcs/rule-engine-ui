# React Company Adapter

Company card adapter for enterprise-specific widgets.

Purpose
Render a metrics card for `company.card` hints with structured props.

Exports
- `registerCompanyAdapter` to register the adapter

When to modify
Add new metric blocks or detail sections.

When not to touch
Do not remove structured output used by dashboards or audits.
