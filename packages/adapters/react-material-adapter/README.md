# React Material Adapter

Material-style input and button adapters for the React renderer.

Purpose
Render `material.input` and `material.button` components with i18n support.

Exports
- `registerMaterialAdapters` to register material adapters

When to modify
Add new material-style components or styling rules.

When not to touch
Do not remove i18n or accessibility attributes from rendered elements.
